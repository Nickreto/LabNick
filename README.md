# Липин Никита БАС1 Лабораторная No1

# Задание 1 ООП

## Задача 1

### Текст задачи

Разработать класс с тремя целочисленными полями. Создать конструктор копирования.
Разработать метод, вычисляющий минимальное из полей. Перегрузить метод ToString() для
формирования строки из полей класса. Реализовать дочерний класс (его содержание предложить
самостоятельно и описать в решении: какой содержательный смысл имеют поля; реализовать
конструкторы; предложить и реализовать 2-3 метода). Протестировать все конструкторы и
другие методы базового и дочернего классов.

### Алгоритм решения задачи

Класс Coordinate
поля:
Целочисленные x, y, z

Конструкторы:
Базовый
Копирования
параметрический(ввод 3 координат)

Гетеры и сетеры для всех полей

Функция Min:
Если поле a < b то m=a иначе m=b
Если поле c < m то m=c иначе ничего
Вовзращаем m

Перегрузка ToString:
Возвращает строку из названий полей и их значений

Дочерний класс Mark
Конструкторы(Для полей родителя вызывают конструктор родителя):
Базовый
Копирования

Гетер имени

Функция Range:
Принимает два объекта класса Mark
Вычисляет расстояние между двумя объектами

Функция Rename:
Позволяет пользователю поменять имя метки

Перегрузка ToString:
Возвращает строку из названия поля метки и обращения к ToString родителя

### Тесты

![Image alt](https://github.com/Nickreto/LabNick/blob/main/Images/Task1.png)


# Задание 2 Перегрузка операций

## Задача 1

### Текст задачи

класс:
RightTriangle
поля:
double a, double b (длины
катетов)
Методы: 
Вычислить площадь треугольника.
Результат должен быть типа double.

### Алгоритм решения задачи

Класс RightTriangle

поля:
Вещественные a и b

Конструкторы:
Базовый
Ввод стороны a и b

Функция Square:
Возвращает площадь треугольника вещественного типа


### Тесты

![Image alt](https://github.com/Nickreto/LabNick/blob/main/Images/Task2-3.png)


# Задание 3 Перегрузка операций

## Задача 1

### Текст задачи

Унарные операции:
++ увеличивает стороны треугольника в 2 раза (чтобы вновь получить
катеты прямоугольного треугольника);
-- уменьшает стороны треугольника в 2 раза (чтобы вновь получить
катеты прямоугольного треугольника);
Операции приведения типа:
double (явная) – результатом является площадь треугольника, если
треугольник существует и отрицательное число в противном случае
bool (неявная) – результатом является true, если треугольник с такими
длинами сторон существует и falseв противном случае.
Бинарные операции:
<= Trianglet1, Trianglet2 – сравнивает площади треугольников
\>= Trianglet1, Trianglet2 - сравнивает площади треугольников

### Алгоритм решения задачи

Оператор ++ возвращает новый объект у которого обе стороны увеличены вдвое
Оператор -- возвращает новый объект у которого обе стороны уменьшены вдвое

Оператор <= Вызывает метод поиска площади обоих треугольников и 
возвращает правду если первый меньше или равен второму
Иначе Ложь
Оператор >= Вызывает метод поиска площади обоих треугольников и 
возвращает правду если второй меньше или равен первому
Иначе Ложь

Явное преобразование объекта в вещественный тип:
Вызывается метод поиска площади если стороны больше нуля
Иначе минус 1

Неявное преобразование объекта в вещественный тип:
Если обе стороны больше 0 то Правда
иначе Ложь
### Тесты

![Image alt](https://github.com/Nickreto/LabNick/blob/main/Images/Task2-3.png)

